<h1>All Tasks</h1>

<ul>
  <% @tasks.each do |t| %>
    <li>
      <%= link_to t.title, task_path(t) %> -- <%= t.details %>. (<%= t.completed %>).
    </li>
  <% end  %>
</ul>

<%= link_to 'Create new Task', tasks_new_path %>
